const banks = require("./banks");
const cardTemplates = require("./cardsTemplates");
const currency = require("./currency");
const transactionDescriptions = require("./transactionDescriptions");

module.exports = {
  banks,
  cardTemplates,
  currency,
  transactionDescriptions
}